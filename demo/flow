Overview of Scenario B.
Focus was on the lineage part -
Trying to answer the questions
- For a given document/dataset, what rag applications are they being used in?
- For a given query, what documents were referenced in the answer to this query

First we need to register documents
Group the documents to facilitate showing the multi-hop

Show the ingestion flow diagram:
https://github.com/briangallagher/data-strat-poc/blob/main/demo/ingest-pipeline-flow.md

Show the pipelines - 3 pipelines one per collection
Talk about Ray Data + Docling etc

Talk about how the OpenLinage Events are sent from the pipelines with a custom SDK

Show Marquez -
4 events fire
- staging area
- plain text chunks with metadata - output from Docling

Each entity (row) in the collection contains:

The 768-dim embedding vector (generated by Granite 125M at ingest time)
The raw chunk text (stored alongside for retrieval display)
All metadata (doc_id, pipeline_run_id, section_path, page_numbers, etc.)
So it's not just embeddings — it's the complete searchable knowledge store. The embedding is the index; the text + metadata is the payload returned at query time.



Then talk about the Chat Applications
2 separate chat applications
1st show deterministic Rag - show the traces - how are the traces captured
Then show the agentic rag and show the traces - how are the traces captured for this
Something on the OGX piece

Then back to the Document Registry to show the provenance.
How do we correlate everything, how do we show the chunks?



Working on next is unity catalog - the arguably most important piece is the
User Model and priveledge system








